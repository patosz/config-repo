server:
  port: 8900
spring:
  jpa:
    hibernate:
      ddl-auto: update
  datasource:
    url: jdbc:mysql://localhost:3306/devoflix?autoReconnect=true&useSSL=false
    username: root
    password: root
